// Update this when cloning the starter into a real project — see package.json's
// own "name"/"description" fields, which should be updated at the same time.
export const APP_NAME = "nextjs-starter";
